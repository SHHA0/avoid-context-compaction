interface:
  display_name: "Avoid Context Compaction"
  short_description: "Track context and preserve task state"
  default_prompt: "Use $avoid-context-compaction to report context usage, save task state at 50% and 80%, and create a handoff only when I request one."
policy:
  allow_implicit_invocation: true
