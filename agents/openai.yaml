interface:
  display_name: "Avoid Context Compaction"
  short_description: "Report context after every task and offer handoffs"
  default_prompt: "Use $avoid-context-compaction to enable basic monitoring in this conversation, report usage after every task, and generate a handoff only when I choose yes."
policy:
  allow_implicit_invocation: true
