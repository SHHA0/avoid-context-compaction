interface:
  display_name: "Avoid Context Compaction"
  short_description: "Warn before compaction and create durable task handoffs"
  default_prompt: "Use $avoid-context-compaction to check context usage and preserve a reliable handoff for this task."
policy:
  allow_implicit_invocation: true
